services:
  - type: web
    name: peopleops-ai
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ELASTIC_URL
        sync: false
      - key: ELASTIC_API_KEY
        sync: false
      - key: SLACK_SIGNING_SECRET
        sync: false
      - key: SLACK_BOT_TOKEN
        sync: false
        
